<%= image_tag(@photo.file.display) if @photo.file.present? %>
<br>
<br>
<%= link_to 'Delete', dashboard_item_photo_path(id: @photo.id, item_id: @item), confirm: 'Are you sure?', method: :delete, class: 'btn btn-danger'  %>
